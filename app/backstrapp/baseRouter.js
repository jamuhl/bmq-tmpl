define([
  'use!underscore',  
  'use!backbone',
  'use!marionette'
],

function(_, Backbone, Marionette) {

    var Router = Backbone.Marionette.AppRouter.extend({
        
    });

    return Router;

});